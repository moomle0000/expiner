# =============================================================================
# expiner — root .env for Docker
# Auto-loaded by `docker compose` (files under this directory). These values
# are substituted into docker-compose.mongo.yml / docker-compose.no-mongo.yml.
# =============================================================================

# ---------------------------------------------------------------------------
# Mongo — in-cluster stack (docker-compose.mongo.yml) — the middleware mongo
# service is reachable by its compose hostname `mongo` on the internal network.
# ---------------------------------------------------------------------------
MONGODB_URI=mongodb://mongo:27017/image-upload

# ---------------------------------------------------------------------------
# Auth secrets (change in any real deployment!)
# ---------------------------------------------------------------------------
SECRET_KEY=change-me-with-a-long-random-string
JWT_EXPIRES_IN=7d

# ---------------------------------------------------------------------------
# Bootstrap admin (seeded at startup — idempotent, existing users untouched)
# ---------------------------------------------------------------------------
BOOTSTRAP_ADMIN_EMAIL=admin@example.com
BOOTSTRAP_ADMIN_PASSWORD=ChangeMe!2026
BOOTSTRAP_ADMIN_NAME=Admin

# ---------------------------------------------------------------------------
# Optional bootstrap regular user
# ---------------------------------------------------------------------------
# BOOTSTRAP_USER_EMAIL=user@example.com
# BOOTSTRAP_USER_PASSWORD=ChangeMe!2026
# BOOTSTRAP_USER_NAME=User

# ---------------------------------------------------------------------------
# Hosts / CORS
# ---------------------------------------------------------------------------
FRONTEND_ORIGIN=http://localhost:3055
NEXT_PUBLIC_API_BASE_URL=http://localhost:5601

# ---------------------------------------------------------------------------
# Published host ports
# ---------------------------------------------------------------------------
BACKEND_PORT=5601
FRONTEND_PORT=3055