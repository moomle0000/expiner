# Used when running the Next.js dev server directly (no Docker).
# In Docker, NEXT_PUBLIC_API_BASE_URL is set by docker-compose from the
# root .env — both as a build-arg (baked) and as a runtime env (read
# per request by pages/api/_config.ts).
NEXT_PUBLIC_API_BASE_URL=http://localhost:5601
